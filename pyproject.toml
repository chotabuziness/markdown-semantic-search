[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "markdown-semantic-search"
version = "1.3.0"
description = "🤖 Markdown Semantic Search CLI - Search your markdown docs naturally without embeddings."
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
authors = [
    { name = "Ezazul Haq" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Topic :: Text Processing :: Markup :: Markdown",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
keywords = ["markdown", "semantic-search", "duckdb", "cli", "no-embeddings", "tfidf"]
dependencies = [
    "duckdb>=1.4.3",
    "requests>=2.32.5",
]

[project.urls]
Homepage = "https://github.com/chotabuziness/markdown-semantic-search"
Repository = "https://github.com/chotabuziness/markdown-semantic-search"

[project.scripts]
md-search = "main:main"

[tool.hatch.build.targets.wheel]
packages = ["src", "main.py"]
